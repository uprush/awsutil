AWS Utils
=========

AWS Utilities.

# BUild
Build jar file.
``` 
gradle build
```

Publish artifacts into `build/repo`
``` 
gradle publish
```

Build and download runtime dependencies into `build/runtime`.
``` 
gradle runtime
```